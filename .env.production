# Production environment variables for Cloudflare Pages
# These are baked into the static export at build time

# Default to demo mode for reliable operation
# Set to 'yahoo' or 'alpha_vantage' via GitHub Actions env vars for live data
NEXT_PUBLIC_MARKET_DATA_PROVIDER=demo
NEXT_PUBLIC_MARKET_DATA_TIMEOUT_MS=10000
NEXT_PUBLIC_MARKET_DATA_MAX_RETRIES=2
NEXT_PUBLIC_MARKET_DATA_BACKOFF_MS=500

# DeepSeek AI Configuration (from .env.local)
DEEPSEEK_API_KEY=sk-e2cb09bf9123495f8220731144652b2c
